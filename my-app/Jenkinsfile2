pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                deleteDir()
                checkout scm

                sh 'cd my-app && mvn --version'
            }
        }
    }
}
