pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                deleteDir()
                checkout scm
                sh 'set'

#                sh 'cd my-app && mvn --version'
            }
        }
    }
}
