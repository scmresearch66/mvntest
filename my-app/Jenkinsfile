node("master") {
  /* checkout scm */

  env.PATH = "/usr/local/bin:${env.PATH}"

  stage('Build site') {
    sh 'my-app/gradlew';
  }

/*  stage('Inside Docker') {
    timeout(60) {
      env.PATH = "/usr/local/bin:${env.PATH}"
      docker.image("centos:6.7").inside() {
        
        sh 'gradlew'
      }
    }
  }
*/
}
